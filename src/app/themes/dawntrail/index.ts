import type { ThemeSpec } from '../types.ts'

/** 金曦（Dawntrail）：**配色待做**。沿用基线主题，见 `heavensward/index.ts` 的说明。 */
export const dawntrailSpec: ThemeSpec = {
  key: 'dawntrail',
  label: '金曦',
  antd: {},
}
