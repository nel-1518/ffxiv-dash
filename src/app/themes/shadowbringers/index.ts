import type { ThemeSpec } from '../types.ts'

/** 暗影（Shadowbringers）：**配色待做**。沿用基线主题，见 `heavensward/index.ts` 的说明。 */
export const shadowbringersSpec: ThemeSpec = {
  key: 'shadowbringers',
  label: '暗影',
  antd: {},
}
