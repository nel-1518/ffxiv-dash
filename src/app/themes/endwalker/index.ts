import type { ThemeSpec } from '../types.ts'

/** 晓月（Endwalker）：**配色待做**。沿用基线主题，见 `heavensward/index.ts` 的说明。 */
export const endwalkerSpec: ThemeSpec = {
  key: 'endwalker',
  label: '晓月',
  antd: {},
}
