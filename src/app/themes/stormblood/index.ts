import type { ThemeSpec } from '../types.ts'

/** 红莲（Stormblood）：**配色待做**。沿用基线主题，见 `heavensward/index.ts` 的说明。 */
export const stormbloodSpec: ThemeSpec = {
  key: 'stormblood',
  label: '红莲',
  antd: {},
}
